{
  "name": "pakfro-site",
  "version": "2.1.0",
  "private": true,
  "description": "Console Dossier — personal site (React, hash-routed, precompiled JSX).",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
